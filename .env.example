# ============================================================
# QuizWeaver Environment Variables
# ============================================================
# Copy this file to .env and fill in values as needed.
# .env is loaded automatically and never committed to git.
#
# IMPORTANT: All of these are OPTIONAL. QuizWeaver works out of
# the box with the Mock provider (zero cost, no API key needed).
# Only set these when you're ready to use a real AI provider.
# ============================================================

# --- LLM Provider API Keys (set ONE of these) ---

# Google Gemini (recommended - has free tier)
# Get your key at: https://aistudio.google.com/apikey
# GEMINI_API_KEY=your-gemini-api-key-here

# OpenAI (GPT-4o, GPT-4, etc.)
# Get your key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-api-key-here

# Anthropic Claude
# Get your key at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# --- Provider Selection ---
# Which provider to use. Options: mock, gemini, gemini-pro,
# vertex, anthropic, vertex-anthropic, openai, openai-compatible
# Default: mock (zero cost, no API key needed)
# LLM_PROVIDER=mock

# --- Database ---
# SQLite is the default (zero-config, local-first).
# Override the SQLite file path:
# DATABASE_PATH=quiz_warehouse.db
#
# PostgreSQL (optional, for schools with IT departments):
# Requires: pip install psycopg2-binary
# DATABASE_URL=postgresql://user:password@localhost:5432/quizweaver
#
# When DATABASE_URL is set it takes precedence over DATABASE_PATH.
# SQLite remains the default when neither is set.

# --- Security ---
# Secret key for Flask sessions. Change this in production.
# SECRET_KEY=change-me-to-a-random-string
